// ============ Motion (motion.dev) — vanilla animation, no bundler ============
// Framer Motion itself is React-only; this is its vanilla-JS sibling, same
// team/API philosophy. Loaded from CDN and re-exported so every module that
// needs it imports from one pinned place instead of repeating the URL.
export { animate } from "https://cdn.jsdelivr.net/npm/motion@13.0.0/+esm";
